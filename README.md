# apt

Armaan's Pre-trained Transformer language model from scratch in PyTorch.

No `torch.nn`, `torch.nn.functional` or `torch.optim`, except `Parameter`, the
container classes and the `Optimizer` base.

    uv run pytest

Tests and handout are from Stanford CS336 assignment 1, MIT licensed.
